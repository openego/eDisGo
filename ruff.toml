line-length = 88
extend-exclude = ["docs"]

[lint]
select = [
    "E",   # pycodestyle errors
    "F",   # pyflakes
    "I",   # isort
    "UP",  # pyupgrade
]
ignore = ["E203"]

[lint.isort]
lines-between-types = 1
known-first-party = ["edisgo"]
